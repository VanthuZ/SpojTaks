package pl_edu_spoj.ZliczaczLiter;

import java.util.Comparator;

public class MyLetterCompare implements Comparator<Character> {

    @Override
    public int compare(Character o1, Character o2) {
        return 0;
        //TODO write own compare, lowercase over uppercase sort
    }
}
